# PDF Components (`src/components/pdf`)

Purpose: wrappers around `react-pdf` worker/document/page rendering.

## What belongs here

- Shared document wrapper components
- Page preview renderers
- PDF rendering setup helpers used by multiple features

## Boundaries

- Keep rendering concerns here.
- Keep business workflows in `home/` or `workspace/` components.
