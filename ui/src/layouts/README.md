# Layouts (`src/layouts`)

Purpose: structural wrappers reused across multiple views.

## What belongs here

- Shared shell containers
- Reusable responsive layout primitives

## Boundaries

- Keep feature behavior in `src/features` or components.
